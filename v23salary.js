/* Moje Finanse 2.3 — pełny pasek wynagrodzenia + PPK */
(function(){
const G=id=>document.getElementById(id),num=v=>+v||0,PLN=v=>F(num(v));
const FULL=['Styczeń','Luty','Marzec','Kwiecień','Maj','Czerwiec','Lipiec','Sierpień','Wrzesień','Październik','Listopad','Grudzień'];
function ensure23(){S.finance||={};S.finance.ppkReturnPct=num(S.finance.ppkReturnPct)||5;S.finance.ppkStateAnnual=num(S.finance.ppkStateAnnual)||240;S.finance.ppkWelcome=num(S.finance.ppkWelcome);S.finance.employerAccidentPct=num(S.finance.employerAccidentPct)||1.67;save()}
function split(gross,net,ppkEmp,ppkEr){let pension=gross*.0976,disability=gross*.015,sickness=gross*.0245,social=pension+disability+sickness,health=Math.max(0,gross-social)*.09,pit=Math.max(0,gross-social-health-ppkEmp-net),erPension=gross*.0976,erDis=gross*.065,accident=gross*num(S.finance.employerAccidentPct)/100,fp=gross*.0245,fgsp=gross*.001,employerBurden=erPension+erDis+accident+fp+fgsp+ppkEr,totalCost=gross+employerBurden;return{pension,disability,sickness,social,health,pit,erPension,erDis,accident,fp,fgsp,employerBurden,totalCost}}
function salaryRows(){let old=S.selectedYear,y=S.selectedYear;Y();let rows=sal().rows.map((r,i)=>{let s=split(num(r.g),num(r.total),num(r.pe),num(r.er));return{...r,...s,i}});S.selectedYear=old;Y();return{y,rows}}
function pct(v,t){return t>0?Math.max(0,Math.min(100,v/t*100)):0}
function stack(r){let parts=[['net',num(r.total)],['social',r.social],['health',r.health],['pit',r.pit],['ppk',num(r.pe)],['employer',r.employerBurden]],total=Math.max(1,r.totalCost);return `<div class="payStack23">${parts.map(([c,v])=>`<i class="${c}" style="width:${pct(v,total)}%" title="${c}: ${PLN(v)}"></i>`).join('')}</div>`}
function renderSalary23(){let sec=G('salary');if(!sec)return;let box=G('salaryDeep23');if(!box){box=document.createElement('div');box.id='salaryDeep23';sec.appendChild(box)}let {y,rows}=salaryRows(),actual=rows.filter(r=>r.actual),gross=actual.reduce((a,r)=>a+num(r.g),0),net=actual.reduce((a,r)=>a+num(r.total),0),employeeSocial=actual.reduce((a,r)=>a+r.social,0),health=actual.reduce((a,r)=>a+r.health,0),pit=actual.reduce((a,r)=>a+r.pit,0),ppkEmp=actual.reduce((a,r)=>a+num(r.pe),0),ppkEr=actual.reduce((a,r)=>a+num(r.er),0),employerCost=actual.reduce((a,r)=>a+r.totalCost,0),takeHome=employerCost?net/employerCost*100:0,totalDeductions=employerCost-net,avgCostPerNet=net?employerCost/net:0;
let rate=num(S.finance.ppkReturnPct)/100/12,ppk=num(S.finance.ppkOpening),stateAnnual=num(S.finance.ppkStateAnnual),welcome=num(S.finance.ppkWelcome),ppkMonths=[];rows.forEach((r,i)=>{ppk=ppk*(1+rate)+num(r.pe)+num(r.er);ppkMonths.push({i,value:ppk,emp:num(r.pe),er:num(r.er)})});let annualContrib=ppkEmp+ppkEr+stateAnnual+(welcome||0),project=(years)=>{let value=Math.max(0,ppk);for(let m=0;m<years*12;m++)value=value*(1+rate)+(ppkEmp+ppkEr)/12+(m%12===11?stateAnnual:0);return value};
box.innerHTML=`<div class="card salaryHero23"><div><small>PEŁNY KOSZT TWOJEJ PRACY — ${y}</small><h2>${PLN(employerCost)}</h2><span>z czego na konto trafiło ${PLN(net)}</span></div><div class="salaryHeroStats23"><div><small>NETTO / KOSZT FIRMY</small><b>${takeHome.toFixed(1)}%</b></div><div><small>1 ZŁ NETTO KOSZTUJE</small><b>${avgCostPerNet.toFixed(2)} zł</b></div><div><small>SKŁADKI + PIT + PPK</small><b>${PLN(totalDeductions)}</b></div></div></div><div class="card"><div class="title">Pasek wynagrodzenia miesiąc po miesiącu <span class="pill">pełny koszt pracodawcy → netto</span></div><div class="payrollScroll23"><div class="payrollGrid23">${rows.map(r=>`<div class="payMonth23 ${r.actual?'actual':'forecast'}"><div class="payMonthHead23"><b>${FULL[r.i]}</b><span>${r.actual?'rzeczywiste':'prognoza'}</span></div><strong>${PLN(r.total)}</strong><small>netto z ${PLN(r.g)} brutto</small>${stack(r)}<div class="payMini23"><span>społeczne <b>${PLN(r.social)}</b></span><span>zdrowotna <b>${PLN(r.health)}</b></span><span>PIT <b>${PLN(r.pit)}</b></span><span>PPK Ty <b>${PLN(r.pe)}</b></span><span>PPK firma <b>${PLN(r.er)}</b></span><span>koszt firmy <b>${PLN(r.totalCost)}</b></span></div></div>`).join('')}</div></div><div class="payLegend23"><span><i class="net"></i>netto</span><span><i class="social"></i>ZUS pracownika</span><span><i class="health"></i>zdrowotna</span><span><i class="pit"></i>PIT</span><span><i class="ppk"></i>PPK pracownika</span><span><i class="employer"></i>koszty pracodawcy</span></div></div><div class="card"><div class="title">Rok narastająco — co zabiera wynagrodzenie</div><div class="salarySplit23"><div><small>BRUTTO</small><b>${PLN(gross)}</b></div><div><small>NETTO</small><b>${PLN(net)}</b></div><div><small>ZUS PRACOWNIKA</small><b>${PLN(employeeSocial)}</b></div><div><small>ZDROWOTNA</small><b>${PLN(health)}</b></div><div><small>PIT</small><b>${PLN(pit)}</b></div><div><small>PPK — TY</small><b>${PLN(ppkEmp)}</b></div><div><small>PPK — FIRMA</small><b class="green">${PLN(ppkEr)}</b></div><div><small>KOSZT PRACODAWCY</small><b>${PLN(employerCost)}</b></div></div></div><div class="card"><div class="title">PPK — portfel i projekcja</div><div class="monthKpis"><div><small>SALDO PROGNOZOWANE</small><b class="green">${PLN(ppk)}</b></div><div><small>WPŁATY W ROKU</small><b>${PLN(ppkEmp+ppkEr)}</b></div><div><small>DOPŁATY PAŃSTWA</small><b>${PLN(stateAnnual+(welcome||0))}</b></div><div><small>ŁĄCZNY DOPŁYW ${y}</small><b>${PLN(annualContrib)}</b></div></div><div class="ppkProjection23"><div><small>ZA 1 ROK</small><b>${PLN(project(1))}</b></div><div><small>ZA 5 LAT</small><b>${PLN(project(5))}</b></div><div><small>ZA 10 LAT</small><b>${PLN(project(10))}</b></div></div><div class="ppkCurve23">${ppkMonths.map(x=>`<div><i style="height:${Math.max(3,ppk?x.value/Math.max(...ppkMonths.map(z=>z.value),1)*90:3)}px"></i><small>${M[x.i]}</small></div>`).join('')}</div><div class="form ppkSettings23"><label>Saldo początkowe PPK<input id="ppkOpening23" type="number" value="${num(S.finance.ppkOpening)}"></label><label>Stopa zwrotu % rocznie<input id="ppkReturn23" type="number" step="0.1" value="${num(S.finance.ppkReturnPct)}"></label><label>Dopłata roczna państwa<input id="ppkState23" type="number" value="${stateAnnual}"></label><label>Wpłata powitalna / dodatkowa<input id="ppkWelcome23" type="number" value="${welcome}"></label><label>Składka wypadkowa pracodawcy %<input id="accident23" type="number" step="0.01" value="${num(S.finance.employerAccidentPct)}"></label></div><button class="btn primary" onclick="saveSalarySettings23()">Zapisz ustawienia etatu i PPK</button><p class="muted">Wyliczenia są orientacyjne. Dokładna lista płac może się różnić przez ulgi, limity ZUS, koszty uzyskania, składkę wypadkową i ustawienia pracodawcy.</p></div>`;
}
window.saveSalarySettings23=function(){S.finance.ppkOpening=num(G('ppkOpening23').value);S.finance.ppkReturnPct=num(G('ppkReturn23').value);S.finance.ppkStateAnnual=num(G('ppkState23').value);S.finance.ppkWelcome=num(G('ppkWelcome23').value);S.finance.employerAccidentPct=num(G('accident23').value)||1.67;save();renderSalary23()};
function version(){let h=document.querySelector('.top small');if(h)h.innerHTML=`<span id="headerYear">${S.selectedYear}</span> • wersja 2.3`;document.title='Moje Finanse 2.3'}
ensure23();const prev=window.render;if(typeof prev==='function')window.render=function(){prev();renderSalary23();version()};setTimeout(()=>{renderSalary23();version()},60);
})();